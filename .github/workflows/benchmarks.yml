name: Run benchmarks
#on:
#  push:
#    tags:
#      - 'v*'
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    name: Run benchmark
    runs-on: [self-hosted, bench]
    steps:
      - uses: actions/checkout@v2.3.3
      - name: Set env
        run: |
          echo "TAG=v0.0.0"
          echo "W_DIR=/home/runner"
          echo "REPO=helm"
          echo "$GITHUB_WORKSPACE"
          rm -rf benchmarks
          git clone https://github.com/artipie/benchmarks.git
          cd benchmarks/adapters
          echo "$REPO"
          make prepare TARGET="$REPO"
